{
  "scripts": {
    "release": "git push origin next && git push origin master && git push dist next && git push dist master"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "node-rubika-bot-api": "https://github.com/vrwebdesign/rubika_bot",
    "redis": "^2.8.0"
  }
}
