{
  "scripts": {
    "release": "git push origin next && git push origin master && git push dist next && git push dist master"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "moment": "^2.24.0",
    "mysql": "^2.17.1",
    "redis": "^2.8.0",
    "rubika-bot": "^1.0.1"
  }
}
